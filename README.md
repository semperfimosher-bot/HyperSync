# HyperSync
A custom streaming app using a data-driven React frontend and an async Python bot. The backend runs 24/7, uses Playwright to safely find tracks on YouTube Music without accounts, and downloads them via yt-dlp to server storage. Music streams with zero latency (&lt;50ms) over secure WebSockets.
